# Python 虛擬環境與快取
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node
node_modules/
dist/
frontend/.vite/
frontend/tsconfig.tsbuildinfo

# 使用者資料（媒體檔與轉錄結果）——備份 = 複製此目錄
data/

# AI 模型（約 4.9GB，首次使用自動下載到 models/hub/，不進版控）
models/

# 執行期檔案（pid、log）
.run/

# 本機 Port 與 HF_TOKEN；預設值與格式見 .env.example
/.env
/.env.*
!/.env.example
# 舊版 Token 路徑仍忽略，避免遷移前誤提交
backend/.env

# Claude Code：第三方 skill 以 `uipro init --ai claude` 重新安裝；個人設定不進版控
.claude/skills/
.claude/settings.local.json

# macOS
.DS_Store
